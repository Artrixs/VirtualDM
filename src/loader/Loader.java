package loader;

import trackinfrastructure.Layout;

public class Loader {
	
	public static Layout load( String file ) {
		Layout layout = new Layout();
		
		//Parser.parse(filepath, layout)
		// First pass with parser, create all the TrackElements and TracksideElements, (store a list of them)
		
		// Second pass to link all elements to each other.
		
		return layout;
	}

}
